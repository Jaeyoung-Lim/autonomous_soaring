function state = setstate(gamma_a, psi_a, phi, pos, vel, v_a)
    state.gamma_a = gamma_a;
    state.psi_a = psi_a;
    state.phi = phi;
    state.pos = pos;
    state.vel = vel;
    state.v_a = v_a;
end